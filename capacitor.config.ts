import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theta.reprogramming',
  appName: 'Theta Reprogramming',
  webDir: 'dist'
};

export default config;
